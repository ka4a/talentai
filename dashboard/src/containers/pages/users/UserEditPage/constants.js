import { defineMessage } from '@lingui/macro';

export const USER_EDIT_SECTIONS = [
  { id: 'details', label: defineMessage({ message: 'Details' }).id },
  { id: 'metadata', label: defineMessage({ message: 'Metadata' }).id },
];
